/**
 * 左侧导航栏的分组顺序。
 *
 * 页面通过 data/types.ts 里的 `group` 字段声明自己属于哪一组
 * （见 data/pages.ts 中每条记录的 group 字段），
 * 组内顺序由页面的 `order` 决定。加页面不需要改这个文件。
 */
export const navGroups: string[] = ["概览", "内容", "项目"];
