# 统一换行符：仓库内统一存 LF，避免 Windows / macOS 之间反复出现整文件改动
* text=auto eol=lf

# 二进制文件不做换行转换
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary

# 锁文件在 diff 中折叠，减少噪音
package-lock.json linguist-generated=true -diff
